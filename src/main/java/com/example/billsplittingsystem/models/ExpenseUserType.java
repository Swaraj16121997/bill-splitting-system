package com.example.billsplittingsystem.models;

public enum ExpenseUserType {
    PAID_BY,    // those who actually did the payment(s) for a particular expense
    HAD_TO_PAY, // those who were supposed to pay for that particular expense
}
